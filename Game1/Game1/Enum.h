#pragma once

enum
{
	Player,
	MAX,
};

enum SceneID
{
	LogoID,
	MenuID,
	EasyStageID,
	NormalStageID,
	HardStageID,
	ExitID,
};

enum DirectoinPoint
{
	UPPOINT,
	DOWNPOINT,
	LEFTPOINT,
	RIGHTPOINT,
};