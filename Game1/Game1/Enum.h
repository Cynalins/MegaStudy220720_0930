#pragma once

enum
{
	Player,
	MAX,
};

enum SceneID
{
	LogoID,
	MenuID,
	EasyStageID,
	NormalStageID,
	ShootStageID,
	ExitID,
};

enum DirectoinPoint
{
	UPPOINT,
	DOWNPOINT,
	LEFTPOINT,
	RIGHTPOINT,
};